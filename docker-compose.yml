# SiteMind Docker Compose

version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend:/app
      - ./logs:/app/logs
    environment:
      - DEBUG=true
    restart: unless-stopped
